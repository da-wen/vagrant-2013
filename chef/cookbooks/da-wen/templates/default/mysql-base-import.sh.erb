#!/bin/bash

echo "Check for base sql to run ..."

for sql in /vagrant/database/mysql/base/*.sql
do
  if [ -f $sql ]
  then
  	echo "run data script $sql"
    mysql -u root -p<%= node['mysql']['server_root_password'] %> < "$sql"
  fi
done

